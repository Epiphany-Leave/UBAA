# 阳光打卡布局调整（2026-09-22）

- 概览和历史记录共用固定底部“去打卡”，不随记录列表滚动。进入填写页后选择学校返回的可提交项目；从历史快照重新进入且没有项目时，通过现有概览查询补齐，不推断分类或项目编号。
- 填写改为全屏分组卡片：运动项目、时间、地点、照片。时间保留手输并支持系统日期/时间选择；继续确认固定底部，校验错误在按钮上方显示。取消释放照片引用。
- 提醒开关只在阳光打卡子页，沿用原持久设置与保存失败处理；首页仍展示已启用的提醒入口。
- 仅变更 Flutter 展示与交互，不新增学校接口，不改认证、路线、本研分流、Core 提交规则，不导入参考截图中的自动生成时间或透明照片规则。

## 验证

- TDD：先将测试入口改为“去打卡”，旧实现按预期失败（`output/log/ygdk-layout-before.log`）；实现后相关 15 项通过（`ygdk-layout-focused.log`）。包含底部固定、历史页重新进入、项目目标传递、空表校验、取消、能力缺失和结果未知。
- 宿主 21 项通过，包括照片选择等待期间注销后不得准备/提交（`ygdk-layout-host-tests.log`）。
- UI analyze、冻结引用 refs、结构 layout-check、敏感扫描及 diff 检查通过。
- 广泛 UI 测试暴露未更新视觉基线（主页、十二功能与响应式三组）以及上一轮签到筛选控件测试仍寻找旧控件；不自动覆盖基线。该运行中的阳光分页筛选失败已修复，定向测试通过。
- `just check` 仍停在 `scripts/tests/maintenance-gates.sh:139`，本机缺少 `zip`；不能宣称全仓或 CI 验收通过，未安装新工具。
- Android 离线 Debug 构建成功，aapt 确认仅 x86_64、应用名称 UBAA。覆盖安装 Pixel 8 成功并确认正常进入主页，未清除应用数据。实际页面由用户继续验证，无真实业务写入。

## 产物与待验

`output/pixel8/UBAA2-pixel8-ygdk-layout-test.apk`。

请在 Pixel 8 检查历史记录滚动时按钮固定、表单与键盘布局、选择时间/照片及取消返回；开关启用后回首页有提醒，关闭后首页隐藏。此次不提供 ARM64 包。
